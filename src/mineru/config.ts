export interface MineruAgentConfig {
  baseUrl: string;
  apiKey?: string;
  timeoutMs?: number;
  pollIntervalMs?: number;
}

export interface MineruStandardConfig {
  baseUrl: string;
  apiKey?: string;
  timeoutMs?: number;
}
