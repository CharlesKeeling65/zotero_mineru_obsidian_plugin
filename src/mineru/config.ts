export interface MineruAgentConfig {
  baseUrl: string;
  apiKey: string;
  timeoutMs?: number;
}

export interface MineruStandardConfig {
  baseUrl: string;
  apiKey: string;
  timeoutMs?: number;
}
